import { CheckCircle2, FileUp, UploadCloud } from 'lucide-react';
import { useState } from 'react';
export default function PdfUploader() { const [file, setFile] = useState(null); const [dragging, setDragging] = useState(false); const selectFile = (event) => setFile(event.target.files?.[0] ?? null); return <div className="grid gap-5 lg:grid-cols-[1.2fr_0.8fr]"><label onDragOver={(event) => { event.preventDefault(); setDragging(true); }} onDragLeave={() => setDragging(false)} onDrop={(event) => { event.preventDefault(); setDragging(false); setFile(event.dataTransfer.files?.[0] ?? null); }} className={`flex min-h-64 cursor-pointer flex-col items-center justify-center rounded-2xl border-2 border-dashed p-8 text-center transition ${dragging ? 'border-blue-500 bg-blue-50' : 'border-slate-300 bg-white/70 hover:border-blue-400 hover:bg-blue-50/50'}`}><input type="file" accept="application/pdf" onChange={selectFile} className="hidden" /><span className="flex h-14 w-14 items-center justify-center rounded-2xl bg-blue-50 text-blue-600"><UploadCloud className="h-7 w-7" /></span><p className="mt-4 font-semibold text-slate-800">Drop a syllabus PDF here</p><p className="mt-1 text-sm text-slate-500">or browse from your computer</p><span className="mt-5 inline-flex items-center gap-2 rounded-lg bg-blue-600 px-4 py-2 text-sm font-semibold text-white shadow-md shadow-blue-200"><FileUp className="h-4 w-4" /> Choose PDF</span>{file && <p className="mt-4 text-xs font-semibold text-blue-600">Selected: {file.name}</p>}</label><div className="rounded-2xl border border-slate-200/80 bg-white/80 p-5 shadow-sm"><p className="text-xs font-bold uppercase tracking-wider text-slate-400">Parser pipeline</p><div className="mt-5 space-y-5">{['Extract document structure', 'Map learning objectives', 'Create draft topics'].map((step, index) => <div key={step} className="flex gap-3"><span className="flex h-6 w-6 items-center justify-center rounded-full bg-emerald-100 text-xs font-bold text-emerald-700">{index + 1}</span><div><p className="text-sm font-semibold text-slate-700">{step}</p><p className="mt-0.5 text-xs text-slate-500">{index === 0 ? 'PDF and OCR supported' : index === 1 ? 'AI-assisted alignment' : 'Review before publishing'}</p></div></div>)}</div><div className="mt-6 rounded-lg border border-emerald-200 bg-emerald-50 p-3 text-xs text-emerald-700"><CheckCircle2 className="mr-1 inline h-4 w-4" />Drafts never publish without review.</div></div></div>; }
