import { Plus, X } from 'lucide-react';
import { useState } from 'react';
export default function TopicForm({ onCreated }) { const [topic, setTopic] = useState({ name: '', module: 'SEF-301', objective: '' }); const [created, setCreated] = useState(false); const submit = (event) => { event.preventDefault(); if (!topic.name.trim()) return; setCreated(true); onCreated?.(topic); }; return <form onSubmit={submit} className="grid gap-5 lg:grid-cols-[1fr_0.8fr]"><div className="space-y-4 rounded-2xl border border-slate-200/80 bg-white/90 p-5 shadow-sm"><div className="flex items-center justify-between"><div><p className="text-xs font-bold uppercase tracking-wider text-indigo-600">Topic builder</p><h3 className="mt-1 text-lg font-bold text-slate-900">Add a learning topic</h3></div><span className="rounded-lg bg-blue-50 p-2 text-blue-600"><Plus className="h-4 w-4" /></span></div><label className="block text-sm font-semibold text-slate-700">Topic name<input value={topic.name} onChange={(event) => setTopic({ ...topic, name: event.target.value })} placeholder="e.g. Dependency injection" className="mt-2 w-full rounded-lg border border-slate-200 bg-white px-3 py-2.5 text-sm text-slate-700 outline-none focus:border-blue-500" /></label><label className="block text-sm font-semibold text-slate-700">Parent module<select value={topic.module} onChange={(event) => setTopic({ ...topic, module: event.target.value })} className="mt-2 w-full rounded-lg border border-slate-200 bg-white px-3 py-2.5 text-sm text-slate-700 outline-none focus:border-blue-500"><option>SEF-301</option><option>DS-204</option><option>DB-210</option></select></label><label className="block text-sm font-semibold text-slate-700">Learning objective<textarea value={topic.objective} onChange={(event) => setTopic({ ...topic, objective: event.target.value })} placeholder="What should students be able to demonstrate?" className="mt-2 min-h-24 w-full rounded-lg border border-slate-200 bg-white px-3 py-2.5 text-sm text-slate-700 outline-none focus:border-blue-500" /></label><button type="submit" className="rounded-lg bg-blue-600 px-4 py-2.5 text-sm font-semibold text-white shadow-md shadow-blue-200 transition hover:bg-blue-700">Create draft topic</button></div><div className="rounded-2xl border border-dashed border-slate-300 bg-white/60 p-5"><p className="text-xs font-bold uppercase tracking-wider text-slate-400">Preview</p>{created ? <div className="mt-5 rounded-xl border border-emerald-200 bg-emerald-50/70 p-4"><div className="flex justify-between"><p className="font-semibold text-slate-800">{topic.name}</p><button type="button" onClick={() => setCreated(false)} className="text-slate-400"><X className="h-4 w-4" /></button></div><p className="mt-2 text-xs font-semibold text-blue-600">{topic.module}</p><p className="mt-3 text-sm text-slate-600">{topic.objective || 'Add an objective to guide the AI mapping.'}</p></div> : <p className="mt-5 text-sm leading-6 text-slate-500">Your draft topic preview and AI alignment suggestions will appear here.</p>}</div></form>; }
