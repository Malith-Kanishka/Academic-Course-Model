import { Check, Keyboard, X } from 'lucide-react';
import { useEffect, useState } from 'react';
import MasteryChart from './MasteryChart';
import TraceDiffViewer from './TraceDiffViewer';
export default function ApprovalModal({ approval, onClose, onDecision }) { const [feedback, setFeedback] = useState(''); const [rejecting, setRejecting] = useState(false); useEffect(() => { const listener = (event) => { if (event.key === 'Escape') onClose(); if ((event.metaKey || event.ctrlKey) && event.key === 'Enter' && approval) onDecision(approval.id ?? approval.approvalId, 'Approved', ''); }; window.addEventListener('keydown', listener); return () => window.removeEventListener('keydown', listener); }, [approval, onClose, onDecision]); if (!approval) return null; const id = approval.id ?? approval.approvalId; const plan = approval.remedialPlan ?? approval.plan ?? []; const submit = async (decision) => { await onDecision(id, decision, decision === 'Rejected' ? feedback : ''); onClose(); }; return <div className="fixed inset-0 z-50 bg-slate-900/30 backdrop-blur-sm" role="dialog" aria-modal="true"><div className="ml-auto flex h-full w-full max-w-6xl flex-col overflow-y-auto border-l border-slate-200 bg-white/95 shadow-2xl shadow-slate-400/30"><header className="flex items-center justify-between border-b border-slate-200 bg-white/80 px-6 py-4"><div><p className="text-xs font-bold uppercase tracking-wider text-indigo-600">Review workspace / {approval.urgency ?? 'Pending'} risk</p><h2 className="mt-1 text-xl font-bold text-slate-900">{approval.studentName ?? 'Student'} <span className="font-normal text-slate-500">· {approval.courseModule ?? approval.module}</span></h2></div><button type="button" onClick={onClose} className="rounded-lg p-2 text-slate-500 hover:bg-slate-100" aria-label="Close review"><X className="h-5 w-5" /></button></header><main className="grid flex-1 lg:grid-cols-[0.85fr_1.15fr]"><div className="space-y-5 border-b border-slate-200 p-6 lg:border-b-0 lg:border-r"><MasteryChart score={approval.masteryScore ?? approval.score} breakdown={approval.breakdown ?? []} /><section className="rounded-xl border border-indigo-200 bg-indigo-50/80 p-5"><div className="flex items-center justify-between"><div><p className="text-xs font-bold uppercase tracking-wider text-indigo-600">AI recommendation</p><h3 className="mt-1 text-lg font-bold text-indigo-950">Remedial plan summary</h3></div><span className="rounded-full bg-white px-2 py-1 text-[10px] font-bold text-indigo-600">{approval.planVersion ?? 'AI Draft'}</span></div><ul className="mt-4 space-y-3 text-sm text-indigo-950">{(Array.isArray(plan) ? plan : [plan]).map((step, index) => typeof step === 'object' ? null : <li key={step} className="flex gap-3"><span className="flex h-5 w-5 shrink-0 items-center justify-center rounded-full bg-indigo-200 text-xs font-bold">{index + 1}</span>{step}</li>)}</ul></section><div className="rounded-xl border border-slate-200 bg-slate-50/80 p-4 text-sm text-slate-600"><p className="font-semibold text-slate-800">Review context</p><p className="mt-2 leading-6">The plan was triggered after the learner missed {approval.missingConcepts?.length ?? 1} assessed concept(s). Confirm the evidence before publishing.</p></div></div><div className="space-y-5 p-6"><div><p className="text-xs font-bold uppercase tracking-wider text-slate-400">Evidence trace</p><h3 className="mt-1 text-lg font-bold text-slate-900">Submission vs expected standard</h3></div><TraceDiffViewer studentAnswer={approval.studentAnswer ?? approval.answer} expectedStandard={approval.expectedStandard ?? approval.rubric} missingConcepts={approval.missingConcepts ?? []} />{rejecting && <textarea autoFocus value={feedback} onChange={(event) => setFeedback(event.target.value)} placeholder="Explain what should be revised before approval..." className="min-h-28 w-full rounded-xl border border-rose-200 bg-rose-50/50 p-3 text-sm text-slate-700 outline-none focus:border-rose-500" aria-label="Rejection feedback" />}<div className="rounded-xl border border-slate-200 bg-white/80 p-4"><p className="text-xs font-bold uppercase tracking-wider text-slate-400">Decision note</p><p className="mt-2 text-sm text-slate-600">Approving activates the remedial plan and records your decision in the audit trail.</p></div></div></main><footer className="flex flex-col-reverse gap-3 border-t border-slate-200 bg-white/80 p-5 sm:flex-row sm:items-center sm:justify-between"><span className="hidden items-center gap-2 text-xs text-slate-400 sm:flex"><Keyboard className="h-3.5 w-3.5" /> Ctrl + Enter to approve</span><div className="flex flex-col-reverse gap-3 sm:flex-row"><button type="button" onClick={onClose} className="rounded-lg border border-slate-200 px-4 py-2.5 text-sm font-semibold text-slate-600 hover:bg-slate-50">Cancel</button><button type="button" onClick={() => rejecting ? submit('Rejected') : setRejecting(true)} className="rounded-lg border border-rose-200 bg-rose-50 px-4 py-2.5 text-sm font-semibold text-rose-700 hover:bg-rose-100">{rejecting ? 'Submit rejection' : 'Reject with feedback'}</button><button type="button" onClick={() => submit('Approved')} className="inline-flex items-center justify-center gap-2 rounded-lg bg-emerald-600 px-4 py-2.5 text-sm font-semibold text-white shadow-md shadow-emerald-200 hover:bg-emerald-700"><Check className="h-4 w-4" />Approve plan</button></div></footer></div></div>; }
