import { Pause, Play, SkipBack, SkipForward, Volume2 } from 'lucide-react';
import { useState } from 'react';

const bars = [28, 42, 34, 66, 48, 78, 55, 88, 64, 44, 72, 52, 92, 58, 38, 69, 46, 80, 56, 32, 61, 74, 49, 87, 42, 66, 52, 76, 40, 58];
export default function AudioPlayerCard({ title = 'SEF-301 · Design Patterns', speaker = 'Dr. A. Fernando' }) {
  const [playing, setPlaying] = useState(false); const [progress, setProgress] = useState(38); const [speed, setSpeed] = useState('1x');
  return <div className="rounded-2xl border border-slate-200/80 bg-white/80 p-5 shadow-md shadow-slate-200/50 backdrop-blur-xl"><div className="flex items-start justify-between gap-3"><div><p className="text-xs font-bold uppercase tracking-wider text-indigo-600">Now reviewing</p><h2 className="mt-1 text-lg font-bold text-slate-900">{title}</h2><p className="mt-1 text-sm text-slate-500">{speaker} · 24 Sep 2026 · 52 min</p></div><span className="rounded-full bg-emerald-100 px-2.5 py-1 text-xs font-bold text-emerald-700">Analyzed</span></div><div className="mt-7 flex h-20 items-center gap-1 rounded-xl border border-slate-200 bg-slate-900 px-4">{bars.map((height, index) => <span key={index} className={`flex-1 rounded-full transition ${index < Math.floor((progress / 100) * bars.length) ? 'bg-gradient-to-t from-blue-500 to-cyan-400' : 'bg-slate-700'}`} style={{ height: `${height}%` }} />)}</div><input type="range" min="0" max="100" value={progress} onChange={(event) => setProgress(event.target.value)} className="mt-3 w-full accent-blue-500" aria-label="Audio progress" /><div className="flex items-center justify-between text-xs text-slate-500"><span>19:46</span><span>52:08</span></div><div className="mt-4 flex items-center justify-between"><div className="flex items-center gap-2"><button type="button" className="rounded-lg p-2 text-slate-500 hover:bg-slate-100" aria-label="Previous segment"><SkipBack className="h-4 w-4" /></button><button type="button" onClick={() => setPlaying(!playing)} className="flex h-10 w-10 items-center justify-center rounded-full bg-blue-600 text-white shadow-md shadow-blue-200 hover:bg-blue-700" aria-label={playing ? 'Pause audio' : 'Play audio'}>{playing ? <Pause className="h-4 w-4" /> : <Play className="ml-0.5 h-4 w-4" />}</button><button type="button" className="rounded-lg p-2 text-slate-500 hover:bg-slate-100" aria-label="Next segment"><SkipForward className="h-4 w-4" /></button><Volume2 className="ml-2 h-4 w-4 text-slate-400" /></div><div className="flex gap-1 rounded-lg bg-slate-100 p-1">{['0.75x', '1x', '1.25x', '1.5x'].map((option) => <button key={option} type="button" onClick={() => setSpeed(option)} className={`rounded px-2 py-1 text-[11px] font-semibold ${speed === option ? 'bg-white text-blue-600 shadow-sm' : 'text-slate-500'}`}>{option}</button>)}</div></div></div>;
}
